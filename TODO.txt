# loading
1. disable creation of a mesh/texture on load, should create an empty mesh/texture first then drag file to load data

# editor features
1. implement exposing of global vars in interpreted scripts
2. undo and redo

# gui features
1. add list of pixels, ability to edit pixels and reloading the texture (new texture editor)
